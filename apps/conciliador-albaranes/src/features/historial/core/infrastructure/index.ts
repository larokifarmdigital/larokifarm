/**
 * Infraestructura del feature `historial`.
 *
 * Sin adapters propios — historial es consumidor: usa
 * `getComparisonRepository()` y `getStorage()` de `shared/core`. Si en el
 * futuro hubiera un adapter exclusivo del historial (p.ej. cliente para
 * exportar a Google Sheets), iría aquí.
 */
export {};
