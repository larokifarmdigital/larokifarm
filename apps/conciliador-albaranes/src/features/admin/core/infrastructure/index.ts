/**
 * Sin adapters propios — admin es consumidor: usa los repos de `shared/core`
 * (`getUserRepository`, `getBusinessRepository`). Si en el futuro hay
 * integración con un IdP externo (Okta, etc.) iría aquí.
 */
export {};
