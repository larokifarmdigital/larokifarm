/**
 * Use cases compartidos entre features (vacío por ahora).
 *
 * Aquí entrarían orquestaciones que combinan ports compartidos
 * (ComparisonRepository + StorageRepository, p.ej.) cuando dos features
 * necesiten el mismo flujo. Mientras solo una feature lo use, mantén el use
 * case dentro de esa feature.
 */
export {};
