/**
 * Sin adapters propios. La única dependencia externa del motor (Gemini)
 * vive dentro del propio `domain/extraerAlbaran.ts` por decisión documentada
 * (excepción aislada y mockeada en los tests).
 */
export {};
