/**
 * Sin use cases compartidos hoy. Los use cases viven en su módulo de dominio.
 * Si emerge un caso cross-dominio (ej. "borrar todo lo de un negocio"), va
 * aquí porque tocaría comparaciones + storage + usuarios a la vez.
 */
export {};
